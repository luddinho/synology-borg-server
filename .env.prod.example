# Production environment example
TZ=Europe/Berlin

# Host SSH port exposed by docker-compose
SSH_HOST_PORT=2222

# Host paths (prod)
SSH_CONFIG_DIR=/volume1/borg-backups-prod/config/ssh
AUTHORIZED_KEYS_FILE=/volume1/borg-backups-prod/config/authorized_keys
BORG_REPOS_DIR=/volume1/borg-backups-prod/repos

# Dedicated production backup service user (non-admin)
BORG_USER=borgprod
BORG_UID=1070
BORG_GID=100
